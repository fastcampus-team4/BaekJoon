/* let filePath = process.platform === "linux" ? 0 : "example.txt";
let input = require("fs")
  .readFileSync(filePath)
  .toString()
  .split("\n")
  .map((e) => e.trim().split(" "));
let N = parseInt(input[0][0]);
let K = parseInt(input[0][1]);
for (let i = 1; i < N; i++) {
  console.log(input[i].map(Number));  
}

console.log(K); */

console.log("비와이");

/* 이것만 하면 된다고 한다^^^^^^ */
